# Taskflow Backend — Production Dependencies
# Generated from actual imports in server.py only.
# Must be kept minimal to avoid dependency conflicts on Python 3.11.

fastapi==0.110.1
uvicorn==0.25.0
gunicorn==21.2.0

# Database
motor==3.3.1
pymongo[srv]==4.5.0

# Data validation
pydantic==2.11.0
python-dotenv==1.0.1

# Google APIs (OAuth, Calendar, Drive)
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.126.0

# HTTP clients
httpx==0.27.0
requests==2.32.2

# Encryption (Drive backup)
cryptography==42.0.5

# Task scheduling (auto-backups, email reminders)
APScheduler==3.10.4

# Email
# (uses smtplib from stdlib — no extra package needed)

# Timezone support
pytz==2024.1
python-dateutil==2.9.0
